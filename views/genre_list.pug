extends layout 
block content 
    h1= title 
    ul
    if genre_list.length 
        each genre in genre_list 
            li 
                a(href=genre.url) #{genre.name} 
                
    else 
        p There is no genre